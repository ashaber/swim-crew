# CLAUDE.md — swim-crew

AI working context for this project. Read this before making any changes.

---

## What This Is

A crew-facing feed/pace tracker for ultra open-water swims. Built for Andrew's wife: 10km swims near-term, a 33.3km swim end of year. Crew feeds the athlete roughly every 30 minutes and logs it here; the app can track **two athletes simultaneously** (swipeable) since Andrew may crew two swimmers at once.

**Primary user:** Crew member on shore/boat — phone in hand, needs a fast, low-cognitive-load interface, possibly with weak/no cell service.

**Modeled on:** `ashaber/pit-sync` (sibling repo at `../pit-sync`) — same philosophy: vanilla JS, ES modules, Vite, Vitest, localStorage-only, dark mobile UI, GitHub Pages deploy. Adapted from pit-sync's **lap-triggered** logging to **30-minute-interval-triggered** feed logging, plus multi-athlete support pit-sync doesn't have.

**Garmin Connect IQ / Edge 540 integration is explicitly out of scope for this phase** — separate Monkey C toolchain, future work. This app is web-only (PWA-style, no backend).

---

## Current State (as of this scaffold)

Initial scaffold generated by Claude — **not yet installed, not yet tested, not yet pushed to GitHub, not yet run in a browser.** Treat everything below as unverified until step 1 of Next Steps is done.

Files in place:
```
index.html          — full UI markup + inline CSS (dark theme, tab bar: Feed / Timeline / Checklist / Plan)
vite.config.js       — mirrors pit-sync's config, base path for GitHub Pages
package.json         — same deps as pit-sync (vite, vitest, jsdom, marked)
swim-plan.md          — placeholder nutrition/pacing content, rendered into Plan tab via marked.js
src/
  app.js             — DOM layer, event handlers, window bindings (mirrors pit-sync/src/app.js)
  storage.js         — localStorage read/write, keys prefixed swim_crew_*
  timer.js           — race clock + getCurrentIntervalNum()/getMsUntilNextFeed() (30-min interval logic)
  athletes.js         — athlete CRUD, swipe/active-athlete resolution (new — pit-sync has no equivalent)
  feeds.js           — replaces pit-sync's laps.js; haversine distance, calcPace (GPS or manual fallback), calcFeedStats
  checklist.js        — swim-specific checklist defaults (feed station, boat/shore, athlete gear, race day)
  timeline.js         — offset-based pre/post-race events, resolved against manual race start (no fixed date like pit-sync)
tests/unit/
  feeds.test.js        — haversine, calcPace (GPS + manual fallback + no-progress case), calcFeedStats
  athletes.test.js     — resolveActiveId, nextAthleteId wraparound
.github/workflows/deploy.yml — npm test → vite build → GitHub Pages, copied from pit-sync
```

Full data model, module-by-module rationale, and UI spec are in the original plan file this was built from (not checked into this repo — ask Andrew if you need the source plan doc).

### Data model quick reference

```js
// Athlete
{ id: uuid, name, targetDistanceKm, active }

// Feed (core object, analogous to pit-sync's Lap)
{
  id, athlete_id, intervalNum, timestamp,
  given: { drink: bool, gel: number, water: bool },
  transitionSec,      // 15s increments
  condition,           // 1-5, emoji scale
  location: { method: 'gps'|'manual', lat?, lng?, distanceM? } | null,
  notes,
}
```

localStorage keys: `swim_crew_athletes`, `swim_crew_feeds`, `swim_crew_race_start`, `swim_crew_race_stopped`, `swim_crew_checklist`, `swim_crew_timeline`, `swim_crew_notes`, `swim_crew_active_athlete`.

---

## Next Steps (do these in order)

1. **Install and test.** `npm install && npm test`. This has never been run — expect to fix real bugs, not just typos. Known risk areas:
   - `src/app.js` DOM wiring (`saveFeed()`, `renderFeedTab()`, `renderAthleteSwipe()`) was written without ever loading in a browser — check the query selectors (`.check-opt[data-id="drink"]`) actually match `index.html`.
   - `timeline.js` — `resolveTimeline()` behavior when `raceStart` is null (pre-swim) needs a manual eyeball check against the UI.
2. **Run it.** `npm run dev`, open in browser, walk the golden path: add athlete → start swim → log a feed → swipe to second athlete → log a feed → check Timeline/Checklist/Plan tabs → export JSON → reload page and confirm localStorage persistence.
3. **GPS test.** `dropGpsPin()` uses `navigator.geolocation` — needs a real device or browser location permission to verify; also verify the manual-distance fallback path when GPS is denied/unavailable.
4. **Create the GitHub repo** (`ashaber/swim-crew`), `git init`, initial commit, add remote, push. `.github/workflows/deploy.yml` expects a `main` branch and GitHub Pages configured for Actions deployment.
5. **PWA pass (optional, later):** pit-sync's sibling `mtb-skills` repo added `vite-plugin-pwa` in Phase 2a — same pattern could apply here for install-to-home-screen + offline pre-cache, once the core app is verified working.

---

## Design Decisions (carried over from pit-sync, or new for this app)

- **No framework, no backend** — same reasoning as pit-sync: offline-first, low complexity, no reactive-state requirements that justify the overhead.
- **30-min interval instead of lap-based** — `timer.js`'s `getCurrentIntervalNum()` divides elapsed time by `FEED_INTERVAL_MS` (30 min), unlike pit-sync's variable-length lap timer.
- **Two-athlete swipe** — `athletes.js` + `app.js`'s swipe/dot UI is net-new; pit-sync only ever tracked one rider.
- **Pace is straight-line, not course-following** — `calcPace()` uses haversine distance between consecutive GPS pins, which undercounts actual swim distance on a non-straight course. This is a known, accepted approximation (crew is estimating from shore, not tracking the swimmer's exact path). Garmin Edge 540 will have the accurate number later.
- **Manual distance fallback** — GPS may fail/be denied; `location.method === 'manual'` lets crew type a cumulative distance estimate instead, and `calcPace()` handles both cases.

## Known Gaps / Deferred

- [ ] Never installed/run — see Next Steps #1-3
- [ ] Not pushed to GitHub — see Next Steps #4
- [ ] No PWA manifest/service worker yet
- [ ] No add/edit UI for athletes beyond initial "Add Athlete" card (no rename, no re-adding a removed athlete's target distance)
- [ ] Garmin Connect IQ / Edge 540 app — separate toolchain, future phase
- [ ] `swim-plan.md` pacing table is placeholder ("TBD") — fill in once target paces are known
